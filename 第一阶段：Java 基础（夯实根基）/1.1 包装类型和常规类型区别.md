在 Java 里，**常规类型（基本类型）** 和 **包装类型** 像两条并行的铁路，一条追求速度，一条追求灵活。看着名字相近，性格却差很多。我们把它们拆开看一眼 

---

## 一、它们分别是谁

### 1️ 基本类型（Primitive Types）

Java 的“原生居民”，一共 8 个：

| 基本类型      | 默认值        |
| --------- | ---------- |
| `byte`    | `0`        |
| `short`   | `0`        |
| `int`     | `0`        |
| `long`    | `0L`       |
| `float`   | `0.0f`     |
| `double`  | `0.0d`     |
| `char`    | `'\u0000'` |
| `boolean` | `false`    |

 特点：

* **不属于对象**
* **直接存值**
* **速度快、内存小**

---

### 2️ 包装类型（Wrapper Classes）

给基本类型穿上“对象外衣”的版本：

| 基本类型      | 包装类型        |
| --------- | ----------- |
| `byte`    | `Byte`      |
| `short`   | `Short`     |
| `int`     | `Integer`   |
| `long`    | `Long`      |
| `float`   | `Float`     |
| `double`  | `Double`    |
| `char`    | `Character` |
| `boolean` | `Boolean`   |

 特点：

* **是对象（继承自 Object）**
* **可以为 null**
* **有方法（`parseInt`、`compareTo` 等）**

---

## 二、核心区别一览表

| 维度        | 基本类型      | 包装类型   |
| --------- | --------- | ------ |
| 是否对象      | ❌         | ✅      |
| 是否能为 null | ❌         | ✅      |
| 默认值       | 0 / false | `null` |
| 存储位置      | 栈         | 堆      |
| 是否有方法     | ❌         | ✅      |
| 集合中使用     | ❌         | ✅      |
| 性能        |  快      |  相对慢 |

---

## 三、自动装箱 & 拆箱（坑点集中营）

### 自动装箱

```java
Integer a = 10;   // int → Integer
```

### 自动拆箱

```java
int b = a;        // Integer → int
```

#### ⚠️ 常见坑：NullPointerException

```java
Integer x = null;
int y = x;   // 💥 NPE
```

> 拆箱时 JVM 试图从 `null` 里掏出一个 `int`，结果掏了个空气。

---

## 四、`==` 和 `equals` 的区别（高频雷区）

```java
Integer a = 127;
Integer b = 127;
System.out.println(a == b);  // true

Integer c = 128;
Integer d = 128;
System.out.println(c == d);  // false
```

### 为什么？

* `Integer` 有缓存池：`-128 ~ 127`
* 小数字复用对象，大数字新建对象

✅ 正确比较方式：

```java
c.equals(d)
```

---

## 五、为什么一定需要包装类型？

### 1️ 集合只认对象

```java
List<Integer> list = new ArrayList<>();
// List<int> ❌ 不存在
```

---

### 2️ 泛型必须是对象

```java
Map<String, Integer> map = new HashMap<>();
```

---

### 3️ 框架 / ORM / JSON

```java
class User {
    private Integer age; // 可以区分：未传(null) vs 0
}
```

Spring、MyBatis、Jackson 都偏爱包装类型。

---

## 六、性能与实践建议（运维 / 后端常用）

### ✅ 用基本类型的场景

* 高性能计算
* 循环、计数器
* 算法、并发核心逻辑

```java
for (int i = 0; i < 1_000_000; i++) {}
```

---

### ✅ 用包装类型的场景

* DTO / VO / Entity
* 数据库字段
* 可选值（需要 null 语义）
* 集合、泛型、反射

```java
private Integer retryCount;
```

---

## 七、一句话总结

> **基本类型负责快，包装类型负责活。**
> 一个像短跑运动员，一个像多面手演员 

如果你愿意，我也可以结合 **Spring Boot / MyBatis / Nacos 配置 / JSON 反序列化**，给你讲「什么时候用 `int` 会出生产事故」。

好，我们直接**用能跑、能编译、能“踩坑”的代码**来练。下面这些示例你可以一个个复制到 `main` 里执行，像做实验一样看结果 

---

# 一、基本类型 vs 包装类型（最基础可编译）

```java
public class PrimitiveVsWrapperDemo {

    public static void main(String[] args) {

        int a = 10;                 // 基本类型
        Integer b = 10;             // 包装类型（自动装箱）

        System.out.println(a);      // 10
        System.out.println(b);      // 10
    }
}
```

✔️ 编译通过
✔️ 自动装箱发生在：`Integer b = 10`

---

# 二、自动拆箱的真实风险（NPE）

```java
public class Demo01 {

    public static void main(String[] args) {

        Integer x = null;

        // 编译通过，但运行时报错
        int y = x;   // NullPointerException

        System.out.println(y);
    }
}
```

💥 运行结果：

```
Exception in thread "main" java.lang.NullPointerException
```

👉 **编译器不报错，运行期才炸**，这是线上事故高发点。

---

# 三、`==` 对比陷阱（Integer 缓存）

```java
public class Demo01 {

    public static void main(String[] args) {

        Integer a = 127;
        Integer b = 127;

        Integer c = 128;
        Integer d = 128;

        System.out.println(a == b); // true
        System.out.println(c == d); // false

        System.out.println(c.equals(d)); // true
    }
}
```

 结论：

* `==` 比的是**对象地址**
* `equals` 比的是**值**

---

# 四、集合中必须用包装类型（编译期直接卡死）

```java
import java.util.ArrayList;
import java.util.List;

public class CollectionDemo {

    public static void main(String[] args) {

        List<Integer> list = new ArrayList<>();
        list.add(1);
        list.add(2);

        for (Integer i : list) {
            System.out.println(i);
        }

        // List<int> list2 = new ArrayList<>(); // ❌ 编译不通过
    }
}
```

📌 泛型只接受对象，基本类型直接被挡在门外。

---

# 五、方法参数：值传递的错觉（包装类型也没特权）

```java
public class MethodParamDemo {

    public static void main(String[] args) {

        int a = 10;
        Integer b = 10;

        changeInt(a);
        changeInteger(b);

        System.out.println(a); // 10
        System.out.println(b); // 10
    }

    static void changeInt(int x) {
        x = 20;
    }

    static void changeInteger(Integer x) {
        x = 20; // 新对象，外面不变
    }
}
```

⚠️ Java **只有值传递**
包装类型也不能“改外面的值”。

---

# 六、包装类型能为 null 的实际意义（业务级）

```java
public class NullSemanticDemo {

    static class User {
        Integer age; // null 表示“未填写”
    }

    public static void main(String[] args) {

        User u = new User();

        if (u.age == null) {
            System.out.println("用户未填写年龄");
        }
    }
}
```

如果这里用 `int age;`
👉 默认就是 `0`，你**永远分不清是 0 还是没填**。

---

# 七、性能差异直观示例（循环中别用包装）

```java
public class PerformanceDemo {

    public static void main(String[] args) {

        long start = System.currentTimeMillis();

        Integer sum = 0;
        for (int i = 0; i < 1_000_000; i++) {
            sum += i; // 自动拆箱 + 装箱
        }

        long end = System.currentTimeMillis();
        System.out.println("Integer 耗时：" + (end - start));
    }
}
```

👉 把 `Integer sum` 改成 `int sum`
你会看到明显的时间差。

---

# 八、最小练习任务（你可以自己改）

你可以试着做这 3 个小练习：

1️⃣ 把某个 `Integer` 改成 `int`，看看哪里编译不过
2️⃣ 手动写一段 `null` 拆箱，观察异常
3️⃣ 在 `List<Integer>` 里放 `null`，循环时拆箱会发生什么

---

## 一句话收尾

> **包装类型不是“更高级”，而是“更危险也更灵活”。**
> 用在“表达业务”，不用在“跑分核心”。

如果你愿意，下一步我可以给你一套 **Spring Boot Controller + DTO + MyBatis** 的真实生产代码，直接告诉你：
👉 **哪一行该用 int，哪一行必须用 Integer，否则必出事故。**
