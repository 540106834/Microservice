Java 的异常像一套“报警系统” 
当程序出现异常情况时，它不会默默崩溃，而是大喊一声告诉你：**这里出问题了！**

先给你一张整体地图，避免只记碎片。

---

#  一、Java 异常体系结构（核心理解）

```
Throwable
 ├── Error        （严重错误，基本不处理）
 └── Exception
      ├── RuntimeException （运行时异常 ⭐⭐⭐⭐⭐）
      └── CheckedException （编译时异常）
```

### ✅ Error

属于 **JVM 级别灾难**。

比如：

* `OutOfMemoryError` → 内存炸了
* `StackOverflowError` → 递归太深

 **原则：不用 catch**
这类问题通常是：

* JVM配置问题
* 内存不够
* 代码设计错误

运维反而更常遇到 

---

# ⭐⭐⭐ 二、最常见的运行时异常（必须熟）

这些是面试 + 工作中出现频率极高的。

---

## 1️ NullPointerException（空指针）

王者级异常 
几乎所有 Java 人都被它教育过。

```java
String s = null;
s.length();   // boom!
```

### 本质：

 你在操作一个 **不存在的对象**

### 如何避免：

* 判空
* Optional
* 不乱返回 null

**经验一句话：**

> 80% 的 Java bug 都和 null 有关系。

---

## 2️ ArrayIndexOutOfBoundsException（数组越界）

```java
int[] arr = new int[3];
arr[3];   // 最大只能到2
```

### 本质：

 访问了不存在的索引。

常见原因：

* for循环写错
* 边界没判断

---

## 3️ ClassCastException（类型转换异常）

```java
Object obj = "hello";
Integer i = (Integer) obj; // 崩
```

### 本质：

 **你以为它是A，其实它是B。**

多态场景特别容易出现。

建议：

```java
if(obj instanceof Integer){
}
```

---

## 4️ NumberFormatException（数字格式错误）

```java
Integer.parseInt("abc");
```

常见于：

* 接口参数
* 配置文件
* 环境变量

 运维写脚本 / 配置中心 时特别常见。

---

## 5️ ArithmeticException（数学异常）

```java
int x = 10 / 0;
```

简单直接：

> 任何数都不能除以0。

---

# ⭐⭐⭐⭐ 三、必须处理的“编译时异常”

Java 有一套严格机制：

 **你不处理，代码都不让编译。**

---

## 1️ IOException

文件 / 网络 / 磁盘。

```java
FileInputStream fis = new FileInputStream("a.txt");
```

必须：

* try-catch
  或
* throws

### 为什么？

因为：

* 文件可能不存在
* 权限不够
* 磁盘坏了

 属于“外部世界的不确定性”。

---

## 2️ SQLException

数据库连接、SQL执行失败。

比如：

* 数据库挂了
* 网络断了
* SQL写错

微服务时代依然高频。

---

## 3️ ClassNotFoundException

动态加载类时：

```java
Class.forName("com.mysql.Driver");
```

如果 jar 不在：直接异常。

运维部署 jar 出错时经常见。

---

# ⭐⭐⭐⭐ 四、运维 / 云原生更容易遇到的异常

给你一点“职业加成视角”。

###  SocketTimeoutException

请求超时：

* 下游慢
* 网络抖动
* 服务雪崩

---

###  ConnectException

连接失败：

* IP错
* 端口没开
* 防火墙挡了

看到它：

 **先别改代码！先排网络！**

---

###  OutOfMemoryError

线上最恐怖的报警之一。

常见原因：

* JVM堆太小
* 内存泄漏
* 大对象

运维 + Java 都要懂。

---

# ⭐⭐⭐⭐⭐ 五、一个“高手级理解”

## ❓为什么 Java 要设计异常？

核心只有一句：

> **把“正常逻辑”和“错误逻辑”分开。**

否则代码会变成：

```
if(失败)
if(失败)
if(失败)
if(失败)
```

像一条铺满减速带的公路 

异常机制让主流程保持顺畅。

---

# ⭐⭐⭐⭐⭐⭐ 六、送你一条极强经验（很多人不知道）

##  判断要不要 try-catch 的黄金法则：

### ✅ 能恢复 → catch

比如：

* 重试
* 返回默认值
* 降级

---

### ❌ 不能恢复 → 抛出去

例如：

* 配置文件损坏
* 核心依赖挂了

👉 让 SpringBoot 直接启动失败。

**比带病运行安全。**

---

如果你愿意，我可以继续给你讲一版更硬核的：

✅ Java 异常最佳实践（高手和普通人的分水岭）
✅ try-catch 的性能真相
✅ 为什么很多架构师“不建议乱 catch”
✅ 自定义异常什么时候用
✅ Spring 如何统一异常处理

直接说：

> 深入异常

我带你进入进阶区 
