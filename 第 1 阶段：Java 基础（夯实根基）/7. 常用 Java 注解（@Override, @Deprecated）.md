好的，我们来系统讲解 **Java 常用注解**，特别是你提到的 `@Override` 和 `@Deprecated`，以及它们在实际开发中的作用和用法。

---

## 1️⃣ 注解（Annotation）概念

**注解**是 Java 的一种 **元数据机制**，可以给类、方法、字段、参数等添加额外信息，用于：

* 编译时检查
* 生成文档
* 运行时反射处理

语法示例：

```java
@Override
public String toString() {
    return "Hello";
}
```

---

## 2️⃣ 常用注解

### 2.1 `@Override`

**作用**：

* 标记方法 **覆盖（重写）父类或接口的方法**
* 编译器会检查，如果方法没有正确重写，会报错

**示例**：

```java
class Animal {
    public void eat() {
        System.out.println("Animal is eating");
    }
}

class Dog extends Animal {
    @Override
    public void eat() { // 必须和父类方法签名一致
        System.out.println("Dog is eating bones");
    }
}
```

✅ 优点：

* 提前发现拼写错误或参数不一致
* 增强代码可读性，表明这是重写方法

---

### 2.2 `@Deprecated`

**作用**：

* 标记 **已过时的类、方法、字段**
* 编译器会提醒使用者尽量避免使用

**示例**：

```java
class OldApi {
    @Deprecated
    public void oldMethod() {
        System.out.println("This method is deprecated");
    }

    public void newMethod() {
        System.out.println("Use this method instead");
    }
}

public class Main {
    public static void main(String[] args) {
        OldApi api = new OldApi();
        api.oldMethod(); // 编译器会警告
        api.newMethod();
    }
}
```

* 可结合 `@Deprecated(since="1.2", forRemoval=true)` 指明版本和是否会移除

---

### 2.3 `@SuppressWarnings`

**作用**：告诉编译器忽略特定警告

```java
@SuppressWarnings("unchecked")
public void oldListExample() {
    List list = new ArrayList(); // 原生类型，通常会警告
}
```

---

### 2.4 `@FunctionalInterface`

**作用**：标记接口为 **函数式接口**，只能有一个抽象方法（用于 Lambda 表达式）

```java
@FunctionalInterface
interface Calculator {
    int calc(int a, int b);
}
```

---

### 2.5 `@SafeVarargs`（Java 7+）

**作用**：抑制可变参数泛型警告

```java
@SafeVarargs
public final <T> void printAll(T... args) {
    for (T t : args) System.out.println(t);
}
```

---

### 2.6 `@Retention` 与 `@Target`（定义注解时用）

* `@Retention`：注解保留策略（源码、类文件、运行时）
* `@Target`：注解可用位置（类、方法、字段、参数）

```java
import java.lang.annotation.*;

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
@interface MyAnnotation {}
```

---

## 3️⃣ 实战经验

1. **`@Override`**

   * 每次重写方法都加，避免低级错误
2. **`@Deprecated`**

   * 标记旧接口或方法，给开发者迁移提示
3. **`@SuppressWarnings`**

   * 用在遗留代码或兼容老版本 API 时
4. **自定义注解**

   * 配合反射或 AOP，可做日志、权限校验、事务管理

---

💡 **总结表格**

| 注解                     | 用途               |
| ---------------------- | ---------------- |
| `@Override`            | 重写父类或接口方法，编译时检查  |
| `@Deprecated`          | 标记过时方法/类，编译器提示   |
| `@SuppressWarnings`    | 忽略特定编译警告         |
| `@FunctionalInterface` | 函数式接口（Lambda 使用） |
| `@SafeVarargs`         | 安全使用泛型可变参数       |
| `@Retention/@Target`   | 定义注解的保留策略和作用目标   |

---

我可以帮你画一张 **常用 Java 注解关系图**，把系统注解、方法级注解、类级注解和自定义注解的用途、作用范围直观展示出来。

你希望我画吗？
